// const searchIconBox = document.querySelector('#searchIconBox');

// * launch filtering when click on the main search btn
searchIconBox.addEventListener('click', () => {
    console.log('click on mainSearchBtn');
    refreshWithNewCriterias();
});
